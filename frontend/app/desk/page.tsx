import { DeskLanding } from "@/components/landing/desk-landing"

// "The Research Desk" — a command-center landing (inspired by founder terminals
// like indieterminal.com) rendered in the Living Manuscript identity. Kept
// reachable here alongside the other experiments (/galaxy, /classic) while the
// scrollytelling hero owns /. Reachable directly regardless of auth state.
export default function DeskPage() {
  return <DeskLanding />
}
