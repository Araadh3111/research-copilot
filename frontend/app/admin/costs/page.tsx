import { AdminCosts } from "@/components/admin/admin-costs"

// Internal cost dashboard (Task 2.1). Auth is the backend ADMIN_KEY entered in
// the page — no public data is exposed without it.
export default function AdminCostsPage() {
  return <AdminCosts />
}
