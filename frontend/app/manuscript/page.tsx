import { ManuscriptLanding } from "@/components/landing/manuscript-landing"

// The prior "Living Manuscript" landing, preserved at a named route when The
// Observatory took over `/` (same pattern as /galaxy and /classic).
export default function ManuscriptPage() {
  return <ManuscriptLanding />
}
