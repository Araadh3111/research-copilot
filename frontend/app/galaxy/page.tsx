import { GalaxyLanding } from "@/components/landing/galaxy-landing"

// The galaxy-field landing experiment, kept reachable here while the "Living
// Manuscript" hero owns `/`. (The original marketing page remains at /classic.)
export default function GalaxyPage() {
  return <GalaxyLanding />
}
