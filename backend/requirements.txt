anthropic==0.34.0
fastapi==0.115.0
uvicorn==0.30.0
requests==2.32.3
python-dotenv==1.0.0
httpx==0.27.0
pydantic==2.13.4
starlette==0.38.6
supabase>=2.15.0
PyJWT[crypto]>=2.8.0
slowapi>=0.1.9
# Text embeddings come from the Voyage API (voyage-3.5-lite @ 512-dim) via plain `requests`
# above — no torch / sentence-transformers, which OOM'd on Railway. Set
# VOYAGE_API_KEY in the environment.
# PDF text extraction for the BYO-PDF library (pure-Python, lightweight).
pypdf>=4.0.0
# Required by FastAPI to parse multipart/form-data (PDF uploads) — without it the
# /library/upload route raises at import and the whole app fails to boot.
python-multipart>=0.0.9
